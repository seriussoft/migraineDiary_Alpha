@model IEnumerable<SeriusSoft.MigraineDiaryMVC.MigraineModels.UnitOfMeasurementModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table alternatingTable">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

	<tbody>
@foreach (var item in Model) {
    <tr class="@TableHelpers.AlternateClass(this.ViewBag, "tableRow","tableAlternateRow")">
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UnitOfMeasurementID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UnitOfMeasurementID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UnitOfMeasurementID })
        </td>
    </tr>
}
		</tbody>

</table>
